#include <iostream>
#include <winsock2.h>
using namespace std;

//VJOSA
// start communication
    while (true)
    {
        char message[BUFLEN];
        printf("Enter message: ");
        cin.getline(message, BUFLEN);

